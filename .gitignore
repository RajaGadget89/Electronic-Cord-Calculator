# ── dependencies & build output ───────────────────────────────
node_modules/
dist/
build/
dev-dist/
*.tsbuildinfo

# vite dev artifacts (config timestamp junk)
*.timestamp-*.mjs

# ── secrets & environment — NEVER COMMIT ─────────────────────
.env
.env.*
*.local
*.pem
*.key
*.p12
*.keystore

# ── logs & os ─────────────────────────────────────────────────
*.log
npm-debug.log*
.DS_Store
Thumbs.db

# ── test / coverage ───────────────────────────────────────────
coverage/
.nyc_output/

# ── vercel ────────────────────────────────────────────────────
.vercel/

# ── editor ────────────────────────────────────────────────────
.vscode/
.idea/

# ── user data exported from the app (may contain job details) ─
9spyre-backup-*.json

# ── copyrighted reference material — DO NOT COMMIT ────────────
*Installation*Book*.pdf
*EIT*Standard*.pdf
uploads/
